setInterval(() => {
    console.log('helloWorld');
}, 100);
console.log("I will run first"); // This line of code runs first and then the setInterval gets invoked.

// process stays alive unless.
// kill Process ctrl+C 
// Unexpected error.